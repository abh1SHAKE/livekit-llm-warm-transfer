# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
starlette==0.27.0

# Data validation / forms
pydantic==2.5.0
python-multipart==0.0.6

# Security / JWT
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
PyJWT==2.8.0

# LiveKit REST SDK (for room + token management)
livekit-api==1.0.5
# livekit-rtc removed — not needed for backend token management on Windows

# LLM Providers
openai==1.3.7
groq==0.4.1

# HTTP client
httpx==0.25.2

# Environment & logging
python-dotenv==1.0.0
structlog==23.2.0

# Optional Twilio integration
twilio==8.10.0

# Data handling
pandas==2.1.3
numpy==1.24.3

# Async support
aiofiles==23.2.1
# (asyncio is built into Python ≥3.4 — no need to install separately)

# Testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1

# Production deployment
gunicorn==21.2.0